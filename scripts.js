if (/Mobi/.test(navigator.userAgent)) {
    // Verifica se o agente do usuário contém "Mobi", que é comum em dispositivos móveis
    alert("Esse sistema não está pronto para ser utilizado em dispositivos móveis!");
  }

